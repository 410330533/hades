# df
```shell
df -h               # 用人们可读的方式展示
df -i               # 展示lnode使用情况
df --total          # 展示瓷盘总利用率
df -T               # 展示文件系统类型
df -t ext2 -t ext4  # 包含特定的文件系统类型
df -x tmpf          # 排除特定的文件系统类型
df /                # 显示一个具体的挂载点磁盘使用情况
df -a               # 显示所有文件系统的信息
```
